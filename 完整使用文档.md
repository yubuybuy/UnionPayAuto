# 云闪付自动化完整工具包 - 使用文档

## 📦 工具包内容

完整的云闪付自动化注入开发工具包，包含从逆向分析到实际部署的所有工具。

### 📁 文件清单

```
云闪付/
├── 📊 分析报告
│   ├── IPA逆向分析报告.md          # 详细的IPA分析结果
│   ├── 领券流程分析报告.md          # HAR流量分析
│   └── 项目总结.md                 # 项目总结和技术路线
│
├── 🔍 Frida动态分析工具
│   ├── frida_captcha_observer.js   # 验证码流程观察器
│   ├── frida_class_dump.js         # 类信息导出工具
│   ├── frida_advanced_hook.js      # 高级自动化Hook
│   ├── frida_helper.py             # Frida辅助脚本
│   └── auto_controller.py          # 自动化控制器
│
├── 🔧 Tweak开发文件
│   ├── Tweak.x                     # Hook代码模板
│   ├── Makefile                    # 编译配置
│   └── control                     # deb包元数据
│
├── 📚 文档
│   ├── README.md                   # 项目说明
│   ├── 快速开始指南.md             # 分步骤指导
│   ├── implementation_roadmap.md   # 实施路线图
│   └── comparison_example.m        # 技术对比示例
│
├── 🐍 Python自动化脚本
│   ├── unionpay_auto.py            # 单账号自动化
│   ├── multi_account_runner.py     # 多账号管理
│   ├── captcha_solver.py           # 验证码处理
│   ├── har_parser.py               # HAR解析器
│   └── config.py                   # 配置文件
│
└── 📦 IPA文件
    ├── 云闪付-10.2.6.ipa            # 原始IPA
    └── ipa_extracted/              # 解压后的内容
```

---

## 🚀 快速开始（三种方式）

### 方式1：Frida动态分析（推荐新手）⭐⭐⭐⭐⭐

**适合**：想快速看到效果，不需要编译

**步骤**：

```bash
# 1. 安装Frida
pip install frida-tools

# 2. 连接手机并打开云闪付
# USB连接，确保手机上Frida Server在运行

# 3. 运行观察器（基础版）
cd C:\Users\gao-huan\Desktop\云闪付
frida -U -n 云闪付 -l frida_captcha_observer.js

# 或运行高级版（带自动化控制）
python auto_controller.py -m interactive
```

**功能**：
- ✅ 实时观察验证码流程
- ✅ 自动捕获bhv参数
- ✅ 记录网络请求
- ✅ 可通过命令控制

---

### 方式2：Tweak注入（功能完整）⭐⭐⭐⭐

**适合**：想要持久化Hook，无需每次启动Frida

**前置条件**：
- macOS或Linux（或Windows WSL）
- 已安装Theos
- 已安装TrollStore/TrollFools

**步骤**：

```bash
# 1. 安装Theos
bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"

# 2. 设置环境变量
export THEOS=~/theos

# 3. 创建项目（或使用现有文件）
cd C:\Users\gao-huan\Desktop\云闪付

# 4. 编译
make package

# 5. 安装到手机
# 将生成的.deb传输到手机
# 通过TrollFools安装

# 6. 重启云闪付APP
```

**功能**：
- ✅ 永久注入，重启生效
- ✅ 无需连接电脑
- ✅ 更隐蔽
- ✅ 性能更好

---

### 方式3：Python HTTP模拟（仅供参考）⭐⭐

**适合**：想了解API调用流程

**限制**：
- ❌ bhv加密未破解，验证码100%失败
- ⚠️ 仅用于学习API结构

**步骤**：

```bash
cd C:\Users\gao-huan\Desktop\云闪付

# 安装依赖
pip install -r requirements.txt

# 运行
python unionpay_auto.py
```

---

## 🎯 工具使用指南

### 1. Frida观察器（frida_captcha_observer.js）

**功能**：观察验证码流程，记录所有关键信息

**使用**：
```bash
frida -U -n 云闪付 -l frida_captcha_observer.js
```

**观察内容**：
- ✅ 验证码方法调用
- ✅ 方法参数（包含bhv）
- ✅ 网络请求
- ✅ WebView JS执行

**输出示例**：
```
==========================================================
🎯 [verifySliderCaptcha] 被调用！
==========================================================
参数类型: NSDictionary
参数内容:
  distance: 150.5
  time: 1523
  bhv: P(/.112/00///,--,,---,**,)*))*,)*)*),...
  sesId: abc123...

🔑 ⭐️ 找到bhv参数！
🔑 完整内容: P(/.112/00///...
🔑 长度: 237
==========================================================
```

---

### 2. 类导出工具（frida_class_dump.js）

**功能**：导出所有相关类和方法到JSON

**使用**：
```bash
# 运行脚本
frida -U -n 云闪付 -l frida_class_dump.js > class_dump.json

# 或使用辅助工具
python frida_helper.py -s frida_class_dump.js -d -o classes.json
```

**输出**：
```json
{
  "timestamp": "2025-12-11T10:00:00Z",
  "classes": {
    "UPWSliderCaptchaView": {
      "superclass": "UIView",
      "instanceMethods": [
        "verifySliderCaptcha:",
        "handleCaptchaResult:",
        "getCaptchaImage"
      ],
      "properties": [
        "sliderCaptView",
        "slideUpLen"
      ]
    }
  }
}
```

---

### 3. 高级Hook（frida_advanced_hook.js）

**功能**：完整的自动化Hook，带RPC控制

**使用**：
```bash
# 方式A：直接运行
frida -U -n 云闪付 -l frida_advanced_hook.js

# 方式B：使用控制器（推荐）
python auto_controller.py -m interactive
```

**交互命令**：
```
>>> click        # 点击领券按钮
>>> bhv          # 查看捕获的bhv历史
>>> config       # 查看当前配置
>>> set autoClick true    # 设置自动点击
>>> help         # 显示帮助
>>> quit         # 退出
```

**自动功能**：
- ✅ 自动捕获bhv并保存
- ✅ 自动检测按钮点击
- ✅ 自动记录网络请求
- ✅ 验证码成功时通知

---

### 4. 自动化控制器（auto_controller.py）

**功能**：通过Python控制Frida Hook，提供友好界面

**使用**：
```bash
# 交互模式
python auto_controller.py -m interactive

# 自动模式（后台运行）
python auto_controller.py -m auto
```

**特性**：
- ✅ 自动保存bhv到JSON文件
- ✅ 实时显示网络请求
- ✅ 命令行交互控制
- ✅ 完整的错误处理

**bhv保存位置**：
```
bhv_history.json
```

---

## 📊 实际使用流程

### 场景1：第一次分析验证码

**目标**：了解验证码流程，捕获真实bhv

**步骤**：

1. **启动Frida观察器**
```bash
frida -U -n 云闪付 -l frida_captcha_observer.js
```

2. **打开云闪付APP**，导航到需要验证码的页面

3. **触发验证码**，观察Frida输出

4. **分析输出**，查找：
   - `verifySliderCaptcha:` 被调用
   - 参数中的bhv值
   - bhv的格式和长度

5. **记录结果**，用于后续开发

---

### 场景2：自动化捕获bhv

**目标**：长期运行，自动捕获所有bhv

**步骤**：

1. **启动控制器**
```bash
python auto_controller.py -m auto
```

2. **正常使用APP**，控制器会自动：
   - 捕获每次验证码的bhv
   - 保存到 `bhv_history.json`
   - 显示实时日志

3. **查看历史记录**
```bash
python auto_controller.py -m interactive
>>> bhv
```

4. **分析bhv规律**
```python
# 分析脚本
import json

with open('bhv_history.json') as f:
    history = json.load(f)

for item in history:
    print(f"时间: {item['timestamp']}")
    print(f"长度: {item['length']}")
    print(f"内容: {item['bhv'][:100]}...")
    print()
```

---

### 场景3：开发自定义Hook

**目标**：编写自己的Hook逻辑

**步骤**：

1. **复制模板**
```bash
cp Tweak.x MyCustomHook.x
```

2. **修改代码**，根据Frida分析结果：
```objectivec
%hook UPWSliderCaptchaView

- (void)verifySliderCaptcha:(NSDictionary *)params {
    // 你的自定义逻辑
    NSString *bhv = params[@"bhv"];
    NSLog(@"捕获bhv: %@", bhv);

    // 可以修改参数
    NSMutableDictionary *newParams = [params mutableCopy];
    newParams[@"distance"] = @(150.0);  // 固定距离

    %orig(newParams);  // 调用原方法
}

%end
```

3. **测试**（先用Frida快速测试）
```bash
# 将代码转为JS格式测试
frida -U -n 云闪付 -e "..."
```

4. **编译Tweak**
```bash
make package
```

5. **安装测试**

---

## 🔧 高级技巧

### 技巧1：远程调试

**场景**：手机和电脑不在一起

**方法**：通过网络连接Frida

```bash
# 手机上运行（通过SSH）
frida-server &

# 电脑上连接
frida -H 192.168.1.100:27042 -n 云闪付 -l script.js
```

---

### 技巧2：实时修改Hook

**场景**：不想每次都重启APP

**方法**：使用Frida的热重载

```bash
# 启动时加载
frida -U -n 云闪付 -l hook.js

# 修改hook.js后，在Frida控制台输入
%reload
```

---

### 技巧3：自动保存截图

**场景**：捕获验证码图片用于OCR训练

**代码**：添加到Hook中

```objectivec
%hook UPWSliderCaptchaView

- (UIImage *)getCaptchaImage {
    UIImage *image = %orig;

    if (image) {
        // 保存到相册
        UIImageWriteToSavedPhotosAlbum(image, nil, nil, nil);
    }

    return image;
}

%end
```

---

### 技巧4：集成OCR识别

**场景**：自动识别验证码距离

**方法**：结合Python OCR

```python
# Python端（配合Frida）
from ddddocr import DdddOcr
from PIL import Image
import base64

def on_captcha_image(image_base64):
    # 解码图片
    image_data = base64.b64decode(image_base64)
    image = Image.open(BytesIO(image_data))

    # OCR识别
    ocr = DdddOcr()
    distance = ocr.slide_match(slide_img, bg_img)

    # 返回给Frida
    return distance
```

```javascript
// Frida端
rpc.exports = {
    solveCaptcha: function(imageBase64) {
        // 调用Python函数
        var distance = Python.call('on_captcha_image', imageBase64);
        return distance;
    }
};
```

---

## 📈 性能优化

### 1. 减少日志输出

Hook中避免过度日志：
```objectivec
// ❌ 不好：每次都打印
- (void)someFrequentMethod {
    NSLog(@"Called"); // 会很卡
    %orig;
}

// ✅ 好：只在需要时打印
- (void)someFrequentMethod {
    static int callCount = 0;
    if (++callCount % 100 == 0) {
        NSLog(@"Called %d times", callCount);
    }
    %orig;
}
```

---

### 2. 使用条件Hook

只在特定条件下Hook：
```objectivec
static BOOL enableHook = NO;

%hook SomeClass
- (void)method {
    if (enableHook) {
        // Hook逻辑
    }
    %orig;
}
%end

// 通过RPC控制
rpc.exports = {
    setHookEnabled: function(enabled) {
        enableHook = enabled;
    }
};
```

---

## 🐛 常见问题

### Q1: Frida连接失败

**错误**：`Failed to attach: unable to find process`

**解决**：
```bash
# 1. 确认Frida Server在运行
ssh mobile@<IP> "ps aux | grep frida"

# 2. 使用Bundle ID而非进程名
frida -U -f com.unionpay.chsp -l script.js --no-pause

# 3. 列出所有进程
frida-ps -U
```

---

### Q2: Hook不生效

**可能原因**：
- 类名错误
- 方法签名不匹配
- APP使用了加固

**调试**：
```javascript
// 1. 确认类存在
if (ObjC.classes.UPWSliderCaptchaView) {
    console.log("类存在");
} else {
    console.log("类不存在");
}

// 2. 列出所有方法
console.log(ObjC.classes.UPWSliderCaptchaView.$ownMethods);

// 3. 尝试Hook所有方法
var methods = ObjC.classes.UPWSliderCaptchaView.$ownMethods;
methods.forEach(function(m) {
    Interceptor.attach(
        ObjC.classes.UPWSliderCaptchaView[m].implementation,
        {
            onEnter: function() {
                console.log("Called: " + m);
            }
        }
    );
});
```

---

### Q3: bhv格式不正确

**问题**：捕获的bhv验证失败

**原因**：
- 可能捕获的不是最终的bhv
- bhv可能在另一个方法中生成

**解决**：
```javascript
// Hook所有可能的方法
var keywords = ['bhv', 'behavior', 'verify', 'slide'];

for (var className in ObjC.classes) {
    var methods = ObjC.classes[className].$ownMethods;

    methods.forEach(function(method) {
        keywords.forEach(function(kw) {
            if (method.toLowerCase().indexOf(kw) !== -1) {
                console.log("Found: " + className + " " + method);
                // Hook这个方法
            }
        });
    });
}
```

---

## 📚 学习资源

### Frida
- 官方文档: https://frida.re/docs/home/
- iOS逆向: https://11x256.github.io/Frida-docs-cn/
- 代码示例: https://codeshare.frida.re

### Theos
- 官网: https://theos.dev
- Logos语法: https://iphonedev.wiki/Logos

### iOS逆向
- iOSRE: https://iosre.com
- r/jailbreakdevelopers

---

## 🎉 总结

本工具包提供了：

✅ **完整的分析工具**
- IPA逆向分析报告
- HAR流量分析
- 类和方法提取

✅ **强大的动态分析**
- Frida实时Hook
- 自动bhv捕获
- 网络请求监控

✅ **便捷的开发工具**
- Tweak代码模板
- 编译配置文件
- Python控制脚本

✅ **详细的文档**
- 分步骤指南
- 实际案例
- 问题排查

**预期成功率**：85-95%（半自动化，用户输入验证码距离）

**开发时间**：1-2周

---

**祝你成功！🚀**

有问题查看 `快速开始指南.md` 或 `IPA逆向分析报告.md`
