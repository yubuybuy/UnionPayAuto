# 云闪付自动领券工具 - 项目总结

## 🎯 项目概述

基于HAR流量分析开发的自动化领券工具，包含完整的框架代码、配置提取工具、多账号支持和详细文档。

**重要提示：** 当前版本中验证码的bhv（行为轨迹）参数使用的是模拟数据，需要逆向分析云闪付的实际加密算法才能完全工作。

---

## 📁 文件说明

### 核心脚本

| 文件名 | 功能 | 状态 |
|--------|------|------|
| `unionpay_auto.py` | 单账号自动领券主脚本 | ✅ 完成 |
| `multi_account_runner.py` | 多账号并发/顺序运行器 | ✅ 完成 |
| `captcha_solver.py` | 验证码识别处理模块 | ⚠️ 需完善bhv加密 |
| `har_parser.py` | HAR文件解析和配置提取 | ✅ 完成 |

### 配置文件

| 文件名 | 功能 | 说明 |
|--------|------|------|
| `config.py` | 主配置文件 | 需用户配置 |
| `requirements.txt` | Python依赖列表 | pip安装 |

### 文档

| 文件名 | 内容 |
|--------|------|
| `README.md` | 详细使用说明 |
| `领券流程分析报告.md` | HAR分析报告 |
| `项目总结.md` | 本文件 |

### 启动脚本

| 文件名 | 平台 |
|--------|------|
| `start.bat` | Windows批处理 |
| `start.sh` | Linux/Mac Shell |

### HAR数据

| 文件名 | 说明 |
|--------|------|
| `ProxyPin12-5_10_07_00.har` | 原始抓包文件 |
| `analysis.txt` | 文本分析结果 |

---

## 🔧 技术架构

### 模块划分

```
┌─────────────────────────────────────────┐
│         用户启动脚本                     │
│  (start.bat / start.sh / 直接运行)      │
└───────────────┬─────────────────────────┘
                │
        ┌───────┴────────┐
        │                │
        v                v
┌─────────────┐  ┌──────────────────┐
│  单账号模式  │  │   多账号模式      │
│unionpay_auto│  │multi_account_    │
│    .py      │  │   runner.py      │
└──────┬──────┘  └────────┬─────────┘
       │                  │
       └────────┬─────────┘
                │
        ┌───────┴────────────┬──────────────┐
        v                    v              v
┌─────────────┐      ┌─────────────┐  ┌──────────┐
│ Config模块  │      │ Captcha模块 │  │  Logging │
│  config.py  │      │captcha_     │  │   模块   │
│             │      │solver.py    │  │          │
└─────────────┘      └─────────────┘  └──────────┘
        │                    │
        │                    │
        v                    v
┌─────────────────────────────────────┐
│          核心业务逻辑                │
│  - 会话初始化 (initses)             │
│  - 验证码处理                       │
│  - 领券请求 (couponAcquire)         │
│  - 重试策略                         │
└─────────────────────────────────────┘
```

### 关键类说明

#### 1. UnionPayCouponBot (unionpay_auto.py)

**职责：** 单账号领券核心逻辑

**主要方法：**
- `init_session()` - 检查会话状态和名额
- `get_captcha_token()` - 完成验证码获取token
- `acquire_coupon()` - 提交领券请求
- `run()` - 主运行循环

**工作流程：**
```python
while retry < MAX_RETRY:
    # 1. 等待名额
    while not init_session():
        sleep(1)

    # 2. 处理验证码
    captcha = get_captcha_token()

    # 3. 领取优惠券
    success = acquire_coupon(captcha)

    if success:
        break
```

#### 2. MultiAccountRunner (multi_account_runner.py)

**职责：** 多账号并发管理

**主要方法：**
- `run_concurrent()` - 并发模式
- `run_sequential()` - 顺序模式
- `run_smart_mode()` - 智能模式（探测+并发）

**并发策略：**
```python
# 使用ThreadPoolExecutor实现并发
with ThreadPoolExecutor(max_workers=3) as executor:
    futures = [executor.submit(run_bot, account)
               for account in accounts]
    for future in as_completed(futures):
        result = future.result()
```

#### 3. CaptchaManager (captcha_solver.py)

**职责：** 验证码识别和处理

**支持的解决器：**
- `DDDDOCRSolver` - 本地ddddocr识别
- `ManualSolver` - 手动输入
- `ThirdPartySolver` - 第三方打码平台

**⚠️ 当前限制：**
```python
def _generate_slide_behavior(self, track):
    # TODO: 需要逆向分析实际的加密算法
    # 当前返回模拟数据，可能导致验证失败
    return "P" + random_chars(200)
```

#### 4. HARParser (har_parser.py)

**职责：** HAR文件解析和配置提取

**提取的信息：**
- Cookie
- 设备ID (device_id)
- 设备指纹 (dfp_id)
- User-Agent
- 地理位置
- 活动ID

---

## ⚙️ 使用流程

### 首次配置

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 提取配置（从HAR文件）
python har_parser.py ProxyPin12-5_10_07_00.har

# 3. 复制配置到config.py
# 手动编辑 config.py，填入必要信息

# 4. 测试运行（单账号）
python unionpay_auto.py
```

### 日常使用

**方式1：使用启动脚本（推荐）**
```bash
# Windows
start.bat

# Linux/Mac
chmod +x start.sh
./start.sh
```

**方式2：直接运行**
```bash
# 单账号
python unionpay_auto.py

# 多账号智能模式
python multi_account_runner.py --mode smart
```

---

## 🚧 已知限制和待完成功能

### ⚠️ 关键限制

1. **验证码bhv参数**
   - **问题：** 当前使用模拟数据，未实现真实的加密算法
   - **影响：** 验证码验证可能失败
   - **解决方案：** 需要逆向分析云闪付APP的加密逻辑
   - **优先级：** 🔴 高

2. **设备指纹有效期**
   - **问题：** DFP_ID可能过期
   - **影响：** 请求被拒绝
   - **解决方案：** 定期重新抓包提取
   - **优先级：** 🟡 中

3. **Cookie时效性**
   - **问题：** Cookie会过期
   - **影响：** 认证失败
   - **解决方案：** 自动检测过期并提示用户
   - **优先级：** 🟡 中

### 📋 待完成功能

- [ ] **验证码bhv加密算法** (P0)
  - 逆向分析APP
  - 实现真实的轨迹加密
  - 测试验证成功率

- [ ] **配置自动刷新** (P1)
  - 检测Cookie/DFP过期
  - 自动提示用户更新
  - 支持配置热重载

- [ ] **通知功能** (P2)
  - 企业微信Webhook
  - 钉钉机器人
  - 邮件通知
  - Telegram Bot

- [ ] **Web管理界面** (P3)
  - 实时运行状态
  - 配置管理
  - 日志查看
  - 统计报表

- [ ] **更多验证码方案** (P2)
  - 集成更多打码平台
  - 优化识别准确率
  - 自动重试机制

---

## 🔬 验证码破解技术路线

### 当前进度：20%

```
[████░░░░░░░░░░░░░░░░] 20%
```

### 已完成
- ✅ 验证码流程分析
- ✅ API接口识别
- ✅ 参数结构理解
- ✅ 基础框架搭建

### 进行中
- 🔄 bhv参数格式分析
- 🔄 加密算法逆向

### 待进行
- ⏳ 算法还原实现
- ⏳ 轨迹真实性优化
- ⏳ 成功率测试验证

### 技术难点

1. **bhv参数分析**
   ```
   示例格式:
   "P(/.112/00///,--,,---,**,)*))*,)*)*),)))..."

   可能包含：
   - 时间戳序列
   - 位置坐标编码
   - 加速度/速度信息
   - 校验和
   ```

2. **逆向方法**
   - 静态分析：反编译APK，查找加密函数
   - 动态分析：Hook关键函数，观察加密过程
   - 流量分析：对比多个成功案例找规律

3. **工具推荐**
   - **静态：** jadx, JEB, IDA Pro
   - **动态：** Frida, Xposed
   - **抓包：** ProxyPin, Charles, Burp Suite

---

## 📊 性能评估

### 理论成功率

基于HAR分析的38次尝试数据：

| 因素 | 影响 | 说明 |
|------|------|------|
| 名额充足度 | 🔴 最大 | 71%失败因名额爆满 |
| 验证码准确率 | 🟡 较大 | 当前bhv未实现，约0% |
| 请求速度 | 🟢 较小 | 可优化1-2秒 |
| 并发账号数 | 🟢 较小 | 线性提升成功率 |

**当前成功率：** ~0%（验证码bhv未实现）
**完善后预期：** 30-50%（名额充足时）

### 优化建议

1. **提高响应速度**
   - 预先准备验证码token池
   - 使用异步IO减少等待
   - 本地缓存会话信息

2. **智能轮询策略**
   ```python
   # 动态调整轮询频率
   if consecutive_failures > 10:
       interval = 2.0  # 减慢
   else:
       interval = 0.5  # 加快
   ```

3. **多账号并发**
   - 3-5个账号并发最优
   - 过多会触发限流
   - 建议错峰1-2秒启动

---

## 🎓 学习价值

### 技术点覆盖

- **网络编程**
  - HTTP/HTTPS请求
  - Cookie管理
  - 请求头伪造

- **异步并发**
  - ThreadPoolExecutor
  - 多线程同步
  - 并发控制

- **数据处理**
  - JSON解析
  - HAR格式分析
  - 正则表达式

- **验证码技术**
  - 滑动验证码原理
  - OCR识别
  - 行为轨迹模拟

- **逆向工程**
  - 流量分析
  - 加密算法逆向
  - APP反编译

### 适用场景

本项目代码和思路可以应用于：
- 其他APP的自动化任务
- 抢票、抢购脚本开发
- 爬虫开发（反爬虫对抗）
- 自动化测试
- 接口压力测试

---

## 📝 后续改进方向

### 短期（1-2周）
1. ✅ 完成基础框架
2. 🔄 破解验证码bhv加密
3. ⏳ 测试验证成功率
4. ⏳ 优化错误处理

### 中期（1个月）
1. 添加Web管理界面
2. 支持更多通知方式
3. 实现配置热重载
4. 增加详细统计功能

### 长期（3个月）
1. 插件化架构重构
2. 支持更多活动类型
3. 机器学习优化策略
4. 分布式部署方案

---

## 💡 核心代码片段

### 1. 智能重试策略

```python
def smart_retry(self, max_attempts=100):
    """智能重试机制"""
    consecutive_fails = 0

    for attempt in range(max_attempts):
        # 检测名额
        if not self.has_quota():
            consecutive_fails += 1
            # 动态调整等待时间
            wait = min(1.0 * (1.2 ** consecutive_fails), 5.0)
            time.sleep(wait)
            continue

        # 尝试领取
        if self.acquire():
            return True

        consecutive_fails += 1

    return False
```

### 2. 并发控制

```python
def run_with_concurrency_limit(self, accounts, max_workers=3):
    """限制并发数避免触发限流"""
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        futures = []
        for i, account in enumerate(accounts):
            # 错峰启动
            time.sleep(i * 0.5)
            future = executor.submit(self.run_account, account)
            futures.append(future)

        for future in as_completed(futures):
            if future.result():
                # 成功后立即停止其他任务
                executor.shutdown(wait=False)
                break
```

### 3. 验证码轨迹生成

```python
def generate_realistic_track(self, distance):
    """生成真实的滑动轨迹"""
    track = []
    current = 0
    mid = distance * 0.8  # 80%处开始减速

    while current < distance:
        # 加速/减速阶段
        if current < mid:
            a = random.uniform(2, 4)  # 加速度
        else:
            a = -random.uniform(3, 5)  # 减速度

        # 计算位移
        v = v0 + a * t
        s = v0 * t + 0.5 * a * t^2

        # 添加随机抖动
        x = current + s
        y = random.randint(-2, 2)

        track.append((x, y, timestamp))
        current = x

    return track
```

---

## 📞 联系与支持

- **问题反馈：** 查看日志文件分析问题
- **功能建议：** 欢迎提出改进意见
- **技术交流：** 研究验证码破解等技术

---

## ⚖️ 法律声明

本项目：
- ✅ 仅供学习研究使用
- ✅ 技术分享和交流
- ❌ 禁止商业用途
- ❌ 禁止非法使用
- ❌ 不提供破解服务

使用本工具产生的任何后果由使用者自行承担。

---

## 🎯 总结

本项目通过HAR流量分析，完整还原了云闪付领券的技术流程，并实现了：

✅ **已完成**
- 完整的自动化框架
- HAR配置提取工具
- 多账号并发支持
- 智能重试策略
- 详细文档和注释

⚠️ **待完善**
- 验证码bhv加密算法（关键！）
- 配置自动更新机制
- 通知和监控功能

**学习价值：** 涵盖了网络编程、并发控制、数据分析、逆向工程等多个技术领域

**实用性：** 完善bhv加密后即可实际使用，理论成功率30-50%

**可扩展性：** 代码架构清晰，易于扩展到其他类似场景

---

*文档版本: v1.0*
*最后更新: 2025-12-05*
